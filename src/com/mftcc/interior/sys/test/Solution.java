package com.mftcc.interior.sys.test;

/**
 * @author shensh
 * @date 2018年4月12日  
 * @version 1.0
 */
public class Solution {
	
	public boolean Find(int target, int [][] array) {
		
		
		return false;
/*		$.ajax({
			url:"<%=request.getContextPath()%>/checkLogin.json",
			data:{opNo:opNo,password:password},
			dataType:"json",
			type:"post",
			success:function(data){
				//debugger;
		 		layer.close(layerIndex);
			  	 alert("3  3"+data.flag)
				 alert("/ "+data.flag);
				if (data.flag=="fail") {
					layer.alert(data.msg,{title:"提示"});
					return;
				}
	 			if(data.flag=="success"){
	 				layer.alert("登录成功",{title:"提示"});
	 				window.location.href = "<%=request.getContextPath()%>/main"; 
	 			}  
	 			  layer.msg(' 加载失败！',  {time:2000} );
	 		//	return false;	// 阻止表单自身的提交
			},
			error:function(e){
				 layer.msg('系统出错！',  {time:2000} );
				   alert("3333"+data.flag);
				   if (data.flag=="fail") {
						layer.alert(data.msg,{title:"提示"});
						//return;
					}  
			}
		});    // ajax 结束
*/
	
	
	}

}
